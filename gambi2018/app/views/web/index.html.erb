<%= render "login" if @current_user.nil? %>
<%= render "dashboard" if @current_user and @current_user.admin? %>
<%= render "tables" if @current_user and not @current_user.admin? and @current_user.table.nil? %>
<%= render "table_info" if @current_user and not @current_user.admin? and @current_user.table %>
